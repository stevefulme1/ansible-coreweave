---
storage_namespace: "default"
storage_state: present

# Block storage PVCs
storage_block_pvcs: []
#   - name: "model-data"
#     size_gi: 100
#     storage_class: "block-nvme-ord1"
#     access_mode: "ReadWriteOnce"

# Shared filesystem PVCs
storage_shared_pvcs: []
#   - name: "shared-data"
#     size_gi: 500
#     storage_class: "shared-hdd-ord1"
#     access_mode: "ReadWriteMany"

# Object storage (via Kubernetes secret)
storage_s3_buckets: []
#   - name: "training-data"
#     secret_name: "s3-creds"
#     endpoint: "object.ord1.coreweave.com"
#     region: "ord1"

# Storage classes to verify
storage_required_classes:
  - "block-nvme-ord1"
  - "block-hdd-ord1"
  - "shared-hdd-ord1"

# Data initialization
storage_init_jobs: []
#   - name: "init-model-data"
#     pvc_name: "model-data"
#     image: "alpine:latest"
#     command: ["sh", "-c", "echo initialized > /data/.ready"]
#     mount_path: "/data"

# Kubernetes API
gpu_kubeconfig: "{{ lookup('env', 'KUBECONFIG') }}"
